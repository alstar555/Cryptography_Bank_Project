#include <string>
#include <cstdint>

class SHA1 {
public:
    std::string operator()(const std::string& message) {
        std::string msg_hashed = message;
        return msg_hashed;

    }

private:
  
};
