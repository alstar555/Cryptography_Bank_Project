#include <string>
#include <cstdint>

using namespace std;

class SHA1 {
public:
    std::string operator()(const std::string& message) {
        string msg_hashed = message;
        return msg_hashed;

    }

private:
  
};
